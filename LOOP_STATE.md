# LOOP_STATE

## Goal: «Зафиксировал 50%, остаток продолжаю удерживать» закрыло всю позицию

Заказчик: «плохо отработал, явно указано что закрыть нужно 50% позиции, а бот взял и всю закрыл.
Косяк, фикси».

## Сообщение и что с ним стало (msg 3003, канал 2088626562, 13.08 07:44)

    #WLD - первая цель взята. Зафиксировал 50% позиции, остаток продолжаю удерживать.

Разбор — ДЕТЕРМИНИРОВАННЫЙ (`ch1-structured`, conf 0.9, без AI): ops `tp_hit`, **`close_remainder`**,
`hold`. Действие `close` → закрыт весь остаток.

## Причина

Шаблон `close_remainder` разрешает зазор между глаголом фиксации и словом «остаток» — он вводился
31.07.2026 ради фразы «остаток ПОЗИЦИИ фиксирую по текущим», где между ними стоит слово. Здесь этот
зазор перепрыгнул **запятую** и склеил два РАЗНЫХ предложения:

    зафиксировал 50% позиции, остаток   ← «(за)фиксир[а-я]*[^.\n]{0,25}остат[а-я]*» матчится

Дальше сработало правило «полное закрытие сильнее доли» — `partial_close` (0.5) был погашен, и от
сообщения осталось «закрыть всё». Слово `hold` («продолжаю удерживать») в ops было, но оно no-op.

## Цена ошибки

Вход 798.4 шорт @ 0.3332. Первая цель отработала НАШИМ же TP-ордером: 266.1 @ 0.3259 → +1.94.
Ошибочное закрытие выкупило остаток 532.3 @ **0.3382** — выше входа, то есть в минус: **−2.66**.
Итог сделки TR-1072: **−1.22** вместо удержания остатка к целям автора (0.3213 / 0.3156).

## Фикс — два независимых гейта

1. **Запятая — граница смысла наравне с точкой** (`[^.,\n]` вместо `[^.\n]` в «остаточных» ветках).
   Фраза, ради которой зазор вводился, запятой внутри не имеет.
2. **Явное удержание отменяет закрытие**: «остаток удерживаю/держу/оставляю/тяну» → `close_remainder`
   не порождается даже без запятой. При этом ДОЛЯ выживает: «зафиксировал 50%, остаток держу» — это
   фиксация половины, а не полный выход (раньше полное закрытие гасило долю безусловно).

Тот же капкан стоял в CH2 (слово «остаток» живёт в гейте выхода `CLOSE_GATE_RE`) — закрыт тем же
правилом.

## Тесты

- `ch1.adapter.test.ts` +2: живая фраза даёт `tp_hit` + `partial_close 0.5` + `hold` и НЕ даёт
  `close_remainder`; варианты удержания без запятой («остаток держу», «остаток оставляю в рынке»,
  «остаток пока тяну дальше»).
- `ch2.adapter.test.ts` +3: то же плюс регрессия «остаток по битку фиксирую» — настоящее закрытие
  остатка по-прежнему закрывает.
- Полный прогон: 908 тестов зелёные (engine 605, api 123, web 87, tg-ingest 58, shared 35).

## Прод

- [x] задеплоено (engine, tg-ingest, api, web)
- [x] позицию по WLD решено НЕ восстанавливать (решение заказчика): сделка остаётся закрытой с
      результатом −1.22, свободная маржа ($27) остаётся под другие сигналы
