# LOOP_STATE

## Goal: «пофикси и открой лимитки» — msg 221765 (19.09.2026 14:03), `needs_review · exchange_rejected_10001`

    Limit long Xrp 1.37 1/2 объема
    Limit long doge 0.085 1/2 объема

## Причина

Правило лимиток ch2 (`scanLimitEntries`) берёт ПОСЛЕДНЕЕ число строки как цену — и это была
«2» из «1/2 объема». Обе лимитки ушли по цене 2:

    [engine] биржа отвергла исполнение сообщения 221765 (retCode=10001):
             StopLoss:182000000 set for Buy position should lower than base_price:142270000

Защитный стоп от цены 2 (1.82) лёг выше рынка XRP (1.42) — биржа отвергла ордер, транзакция
откатилась целиком. Отказ спас от худшего: лимитный Buy по 2 при рынке 1.42 исполнился бы сразу
по рынку.

## Фикс (f445ef9)

Перед поиском цены лимитки из сегмента вырезаются доли объёма: дроби (`1/2`, `1 / 3`) и проценты
(`50%`, уже существовавший `PERCENT_NUM_RE`).

## Тесты

- `ch2.adapter.test.ts` +1: живой текст 221765 + «Limit long btc 60850 50%» → цены 1.37 / 0.085 / 60850
  (до фикса: 2 / 2 / 50).
- Полный прогон движка: 665 зелёных, `tsc` чистый.

## Прод

- [x] задеплоено (f445ef9, engine)
- [x] сообщение 221765 переразобрано самим пайплайном (status → received), `executed`:

      TR-1114  XRP   Buy 510.9 @ 1.37    стоп 1.2467   K02-221765-00-E0  New
      TR-1115  DOGE  Buy 8235  @ 0.085   стоп 0.07757  K02-221765-01-E0  New

      Обе ниже рынка (1.4292 и 0.0896). Стопы защитные — в сообщении их не было.
- Свободная маржа после выставления 181$ из 471$ equity.

## Нюансы деплоя

- Репозиторий на сервере принадлежит root: `git pull` — только через `sudo`. Первая попытка без
  sudo упала, пайп скрыл код ошибки, и `docker compose up -d --build` пересобрал все сервисы на
  прежнем коде (безвредно — просто рестарт).

## Замечено (НЕ чинил)

- «1/2 объема» не учтено: обе лимитки на полный `trade_size` (70$ маржи, 700$ объёма). Доли
  объёма во входе домен не поддерживает (известная задача).
