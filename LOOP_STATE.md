# LOOP_STATE

## Goal: «открой эти лимитки мануально, AI позже пофикшу» — msg 221771, `needs_review · ai_unavailable`

    На закрытые половины ставлю лимитки для доливки - Xrp 1.358, doge - 0.0835

Модель недоступна (не оплачена подписка), а детерминированный разбор эту строку не берёт: в ней
нет слова направления (`long`/`лонг`), гейт `DIR_WORD_GATE_RE` не проходит. Фикс разбора не
делался — заказчик: «AI позже пофикшу».

## Доставка (вручную, но ПОРТОМ бота)

Сообщение проведено настоящим `processMessage` с явной формулировкой «Limit long Xrp 1.358 /
Limit long doge 0.0835» — тем же путём, которым бот прошёл бы сам. Позиции по обоим символам
открыты, поэтому `resolveBusySymbol` превратил лимитные входы в ступени ДОЛИВКИ:

    [pipeline] msg 221771 XRPUSDT: символ занят сделкой dc21977c → add
    [pipeline] msg 221771 DOGEUSDT: символ занят сделкой 65014071 → add

    K02-221771-00-A0  XRP   Buy 515.4 @ 1.358    New
    K02-221771-01-A0  DOGE  Buy 8383  @ 0.0835   New

Обе ниже рынка (1.4029 и 0.08672), по 700$ объёма (`trade_size` 70$ × плечо 10). Синтетическая
строка `needs_review` у сообщения удалена перед разбором (иначе дедуп действий счёл бы её
исполненным действием); `normalized_text` возвращён по настоящему тексту, `status_reason =
manual_delivery_ai_unavailable`, у действий — `detail` про ручную доставку.

## Найденное по пути: модель поняла стоп НАОБОРОТ

Msg 221770 (20.09): «Доге тоже фиксанул как хомяк вместо первой / **Везде без стопа в бу** /
Наоборот буду доливать». Модель выдала `sl_breakeven` по DOGE, BTC и XRP — то есть ровно то, от
чего автор отказался. Стопы встали в безубыток: XRP 1.37, DOGE 0.085, BTC 76879 — ВЫШЕ новых
лимиток-доливок, стоп сработал бы раньше доливки.

Решение оператора: снять стопы совсем. Снято портом исполнения (`cancelOrder` по sl-ордеру =
`trading-stop stopLoss='0'`), заодно закрыты устаревшие sl-строки прежних защитных стопов
(XRP 1.2467, DOGE 0.07757, BTC 70347.8), висевшие в журнале `submitted`.

    BTCUSDT  0.001   @ 76878.95  stopLoss ''  (ликвидация 69420.2)
    XRPUSDT  170.3   @ 1.37      stopLoss ''  (ликвидация 1.2392)
    DOGEUSDT 1373    @ 0.085     stopLoss ''  (ликвидация 0.07708)

Живыми ордерами остались только доливки (BTC 77400, BTC 79500, XRP 1.358, DOGE 0.0835) и тейки
(XRP 1.424, DOGE 0.0884).

## Прод

- [x] обе лимитки выставлены, сообщение 221771 → `executed`
- [x] стопы в бу сняты с трёх позиций
- Свободная маржа 306$ из 497$ equity (каждая доливка резервирует ~70$)

## Осталось (НЕ чинил)

1. **ai-прокси без подписки** — сообщения свободного текста уходят в `needs_review(ai_unavailable)`.
2. **Отрицание у стопа**: «без стопа в бу» модель читает как «стоп в бу». Тот же класс, что и
   «остаток удерживаю» в лексиконе CH1 — отрицание рядом с маркером действия.
3. Доли объёма во входе (`1/2 объема`, «на закрытые половины») домен по-прежнему не поддерживает —
   доливка идёт полным `trade_size`.
