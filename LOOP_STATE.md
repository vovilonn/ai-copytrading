# LOOP_STATE

## Goal: сигнал по LDO пропущен как «нет маржи», хотя маржа была

Заказчик: «по LDO позицию открой из последнего сообщения Артемьева, маржа освободилась» → на мой
ответ «свободной маржи 0» возразил: «ты не прав, сейчас 21 бакс маржи есть». Возражение оказалось
верным, и за ним стоял системный дефект.

## Что показывала биржа (счёт 14.08.2026)

| поле | значение |
|---|---|
| `walletBalance` | **269.85** |
| `equity` | 225.16 (= кошелёк + плавающий PnL −44.69) |
| `totalPositionIM` | 248.10 |
| `totalAvailableBalance` | `""` — Bybit на UNIFIED его не отдаёт вовсе |

Наша формула: `equity − IM` = 225.16 − 248.10 → отрицательно → **0**.
Формула биржи: `walletBalance − IM` = 269.85 − 248.10 = **21.76** — ровно то, что видел заказчик.

## Причина

Плавающий убыток по открытым позициям доступную маржу у Bybit **не съедает**: реализованный убыток
уже сидит в `walletBalance`, а нереализованный — нет. Мы вычитали его ВТОРИЧНО (через equity) и в
просадке переставали торговать вовсе — ровно тогда, когда сигналы и нужны. Сигнал по LDO
(msg 3009) ушёл в `insufficient_margin` при живой марже на полный размер сделки.

**Фикс** (`rest-client.ts::resolveAvailableBalance`): доступно = `walletBalance − positionIM −
orderIM`. Плавающий ПЛЮС по-прежнему не добавляем — до фиксации биржа его тоже не берёт, а ошибка
в меньшую сторону дёшева (войдём меньшим объёмом), в большую — отказ 110007.

## Тесты

- `bybit-rest.test.ts`: ожидания переведены на баланс кошелька + новый кейс «позиции в просадке:
  плавающий минус доступную маржу НЕ съедает» на живых числах прода (21.75772757).
- Полный прогон: 938 тестов зелёные (engine 635, api 123, web 87, tg-ingest 58, shared 35).

## Прод

- [x] задеплоено (engine, tg-ingest, api, web)
- [x] сообщение 3009 переразобрано штатным путём (не скриптом) — фикс проверен на живом пайплайне
- [x] **TR-1075: LDOUSDT short 542.7 @ 0.3003**, плечо 8.15 (из авторского стопа 0.3332), маржа
      20.18$ — ровно `trade_size` канала. Лесенка автора: 180.9 на 0.2910 / 0.2823 / 0.2746,
      стоп 0.3332

Открытые позиции после входа: LDO 20.18 + ICP 20.29 + XRP 100.89 + BTC 77.59 + ETH 49.32 маржи.
